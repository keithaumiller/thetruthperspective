{# filepath: /workspaces/thetruthperspective/newsmotivationmetrics/templates/page--metrics.html.twig #}
{#
/**
 * @file
 * Full-width page template for the metrics dashboard.
 *
 * Available variables:
 * - logged_in: A flag indicating if user is logged in.
 * - root_path: The root path of the current page.
 * - node_type: The content type for the current node, if the page is a node.
 * - head_title: List of text elements that make up the head_title variable.
 * - page: The rendered page content.
 * - directory: The directory the template is located in.
 * - is_front: A flag indicating if the current page is the front page.
 * - logged_in: A flag indicating if the user is registered and logged in.
 * - db_offline: A flag indicating if the database is offline.
 */
#}

<div class="layout-container">
  <header class="header" role="banner">
    {{ page.header }}
  </header>

  {{ page.primary_menu }}
  {{ page.secondary_menu }}

  <main class="main" role="main">
    <a id="main-content" tabindex="-1"></a>{# link is in html.html.twig #}

    <div class="layout-content" style="max-width: none; width: 100%; padding: 0;">
      {{ page.breadcrumb }}

      <div class="region region-content" style="max-width: none; width: 100%; margin: 0;">
        {{ page.highlighted }}
        {{ page.help }}
        {{ page.content }}
      </div>

      {{ page.sidebar_first }}
      {{ page.sidebar_second }}

    </div>
  </main>

  {% if page.footer %}
    <footer class="footer" role="contentinfo">
      {{ page.footer }}
    </footer>
  {% endif %}

</div>